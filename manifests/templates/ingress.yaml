---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hy                  # Ingress name
  labels:
    app: hy
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "hy.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hy               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
